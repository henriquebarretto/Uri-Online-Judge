#include <stdio.h>

int main() {

    char nome[100];
    double salario, vendas;

    scanf("%s %lf %lf", &nome, &salario, &vendas);

    printf("TOTAL = R$ %.2f\n", salario + 0.15*vendas);

    return 0;
}
